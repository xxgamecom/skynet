# source file list
AUX_SOURCE_DIRECTORY("${SKYNET_ROOT_PATH}/skynet-luaclib/sproto" SPROTO_SRC)

# target shared lib
ADD_LIBRARY(sproto MODULE  ${SPROTO_SRC})
SET_TARGET_PROPERTIES(sproto 
    PROPERTIES
    PREFIX ""
    ARCHIVE_OUTPUT_DIRECTORY "${SKYNET_LUACLIB_BIN_PATH}"
    LIBRARY_OUTPUT_DIRECTORY "${SKYNET_LUACLIB_BIN_PATH}"
)
TARGET_LINK_LIBRARIES(sproto liblua)
